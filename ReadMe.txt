Assumptions:

User, Newsletters always exists
All required parameters are always passed in newsletter and user creation methods

Subscrition to newsletter always succeeds as user and newsletter exist
If a new newsletter or user is being created, they dont exist already .
price is an integer , currency is rs.